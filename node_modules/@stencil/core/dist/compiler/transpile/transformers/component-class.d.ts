import { BuildConfig, Diagnostic, ModuleFiles } from '../../../util/interfaces';
import * as ts from 'typescript';
export declare function componentClass(config: BuildConfig, moduleFiles: ModuleFiles, diagnostics: Diagnostic[]): ts.TransformerFactory<ts.SourceFile>;
